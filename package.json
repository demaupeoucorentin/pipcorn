{
  "name": "youtube-pip-extension",
  "version": "1.0.0",
  "description": "Chrome extension for YouTube Picture-in-Picture functionality",
  "scripts": {
    "build": "tsc && npm run copy-files",
    "copy-files": "cp manifest.json dist/ && cp popup.html dist/ && cp popup.css dist/ && cp styles.css dist/ && cp -r icons dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "typescript": "^5.0.0"
  },
  "keywords": ["chrome-extension", "youtube", "picture-in-picture", "typescript"],
  "author": "Your Name",
  "license": "MIT"
}
